apiVersion: apps/v1
kind: Deployment
metadata:
  name: clientthree
  namespace: clientthree
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clientthree
  template:
    metadata:
      labels:
        app: clientthree
        type: agent
      annotations:
        "linkerd.io/inject" : enabled
    spec:
      # nodeSelector:
      #   kubernetes.io/hostname: clientthree
      # nodeName: clientthree
      serviceAccountName: job-creator-clientthree
      containers:
      - name: clientthree
        image: {{ .Values.dockerArtifactAccount }}/agent:{{ .Values.branchNameTag }}
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: DATA_STEWARD_NAME
          value: clientthree
        - name: OC_AGENT_HOST
          value: {{ .Values.tracingEndpoint }}
      - name: sidecar
        image: {{ .Values.dockerArtifactAccount }}/sidecar:{{ .Values.branchNameTag }}
        imagePullPolicy: Always
        env:
        - name: AMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbit
              key: password
        - name: AMQ_USER
          value: normal_user
        - name: OC_AGENT_HOST
          value: {{ .Values.tracingEndpoint }}

---

apiVersion: v1
kind: Service
metadata:
  name: clientthree
  namespace: clientthree
spec:
  selector:
    app: clientthree
    type: agent
  ports:
    - name: http-clientthree-api
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clientthree-ingress
  namespace: clientthree
  annotations:
    nginx.ingress.kubernetes.io/service-upstream: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: clientthree.clientthree.svc.cluster.local
    http:
      paths:
      - pathType: Prefix
        path: "/agent/v1/vflTrainRequest/clientthree"
        backend:
          service:
            name: clientthree
            port:
              number: 8080

---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: clientthree-pv
spec:
  capacity:
    storage: 10Gi
  storageClassName: standard
  accessModes:
    - ReadWriteMany
  hostPath:
      # local path on node
    path: "/mnt/data"
  persistentVolumeReclaimPolicy: Retain
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - clientthree

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clientthree-pvc
  namespace: clientthree
spec:
  volumeName: clientthree-pv
  accessModes:
    - ReadWriteOnce   # Needed for multiple pods to mount simultaneously
  resources:
    requests:
      storage: 5Gi    # Adjust size as needed
  storageClassName: standard
